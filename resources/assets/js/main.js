import React from 'react';
import ReactDOM from 'react-dom';

const app = (
        <h1>
            This is the DLBarbecue API.
        </h1>
);

ReactDOM.render(app, document.getElementById('app'));
